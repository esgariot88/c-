int size = 6;                      
double[] numbers = new double[size];
RealArrayRandomNumbers(numbers);    
              

double max = numbers.Max();
double min = numbers.Min();

void RealArrayRandomNumbers(double[] numbers)
{
for (int i = 0; i < numbers.Length; i++)
{
numbers[i] = new Random().NextDouble();
}
}

Console.Write("Максимальный элемент :"+max+" ");
Console.Write("");
Console.Write("минимальный элемент :"+min);
Console.Write(" Разница между максимальным и минимальным элементами массива= " + (max-min));
